[project]
name = "propensity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JuanVille15", email = "juanville9915@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
propensity = "propensity:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
